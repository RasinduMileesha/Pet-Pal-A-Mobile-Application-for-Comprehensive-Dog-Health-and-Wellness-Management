<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When the image is pressed -->
    <item android:drawable="@drawable/owner_selector" android:state_pressed="true" />
    <!-- Default image (this should be either owner or vet, not both) -->
    <item android:drawable="@drawable/owner" /> <!-- Keep only one default image -->
</selector>
